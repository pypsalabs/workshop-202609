# Builds the single-user notebook image and pushes it to GHCR.
# ONE-TIME manual step after the first run: make the GHCR package
# `workshop-202609-notebook` public (org -> Packages -> package -> settings ->
# Change visibility) so the Elestio VM pulls it without credentials.
name: Build notebook image

on:
  push:
    branches: [main]
    paths:
      - docker/**
      - pixi.toml
      - pixi.lock
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Image metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/pypsalabs/workshop-202609-notebook
          tags: |
            type=raw,value=latest
            type=sha

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: docker/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
